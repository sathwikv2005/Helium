class Node {
    init(value, next) {
        this.value = value;
        this.next = next;
    }
}

const N = 1000000;

var head = null;

var i = 0;
while (i < N) {
    head = Node(i, head);
    i = i + 1;
}
